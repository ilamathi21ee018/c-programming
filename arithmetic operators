#include <stdio.h>

int main()
{
    struct students
    {
        char name[50];
        int age;
        float marks;
    };
    struct students s1={"diba",19,8.1};
    struct students s2={"mathi",18,9.1};
      printf("s1 Details\n");
      printf("%s\n",s1.name);
    
}
// #include <stdio.h>

int main()
{
 int a;
 printf("the value of a is:");
 scanf("%d",&a);
 int b=a*a*a;
 printf("the result is;%d",b);
 return 0;
}
